# Node Test Fixture

Small package for manually testing this adapter against Node's built-in test runner.

From this directory:

```sh
npm test
```

From Neovim, open one of the files in `test/` and use your Neotest mappings.
